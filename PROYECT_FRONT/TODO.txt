- armar la api y testear ahi con los modulos.


pensando.

uno va a importar de models.js:
packet<class>

uno va a importar de auth.js:

las funciones: 
status<dict> = register(user,email,password)
status<dict> = login(user,email=opcional,password)
status<dict> = unlogin()
packet<objt> = sendStateless(url,packet)
packet<objt> = sendStateful(url,packet)

auth.js de trasfondo va a llevar:
- register y login las peticiones (casi estan hechas)
- register y login guardar las sesiones en lugares seguros
- unlogin -> ver si hay una sesion activa y mandar a pedir desactivacion.
- sendStateful y sendStateless
  + usar el tipo de sesion correspondiente que haya.
  + cuando no hay sesion avisar.
  + cuando la sesion se vencio refrescarla.
    * esto va a checkear que el AT que tenemos este vencido antes de enviar siempre.
  + si no se puede refrescar avisar.
  + enviar peticion y avisar si falla. 
- sendStateless
  + cambiar aes en cada envio.
- StatefulEnabled
  + la sesion debe ser mantenida por el front, debe poner un timer y dos minutos antes de que termine refrescarla.


TODO.:
* Agregar un StatefulEnabled=true // para que el FW verifique si esta o no habilitado a usar, y mantener en local los tiempos para la sesion. [DONE]
* Agregar un StatefulTimeSession=10 // (minutos) [DONE]
* Hacer una Class Session para mantener los datos de la sesiones de login y register.[DONE]
* Hacer un timer para el StatefulEnabled que lo comience el register y o el login.[DONE]
* Ver idas y llegadas por consola del refresh
* Hace funciones SSF y SSL en auth. 
* Testear todo.

PLANING.:

para refresh statefull->
* envia el paquete cargando el user_id y el aes(iv,aes(cyphered)) y el JWT.
* recibe mensaje cifrado con la aes vieja y dentro de el va a tener la aes nueva.
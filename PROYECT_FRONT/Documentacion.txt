El front va a poseer las siguientes funcionalidades.

vamos a tener funciones. 
> login, unlogin, register, sendStateless, sendStateful.
login(url,)

tiene que.
- gestionar los tokens de forma automatica
- mantener las sesiones activas automaticamente.
- guardar las variables en sitios seguros.



Documentacion:

En auth.js tenemos funciones heredables:
funcs:
- login(email, username, password)
- register(email, username, password) 
- logout()
- sendStateful(url, data = {}, files = []) -> json
- sendStateless(url, data = {}, files = []) -> json

En Env.js vamos a tener la variables/configuraciones necesarias para el FW.
Va a haber una url para el sistema de auth, o sea las funcs login,register y logout.
Lo que es las funciones sends se les va a pasar URL completa por si es otra URL el back.